Any file in a subdirectory here will override the default in the template
directory. Style names are limited to 'a'..'z', 'A'..'Z', '0'..'9' and '-'.

Files in this directory following the *.inc format mark additional CSS 
layout style sheets that can be combined with any color style.
